<manifest xmlns:android="http://schemas.android.com/apk/res/android"
    xmlns:tools="http://schemas.android.com/tools">

    <uses-permission android:name="android.permission.INTERNET" />

    <application
        android:allowBackup="true"
        android:dataExtractionRules="@xml/data_extraction_rules"
        android:fullBackupContent="@xml/backup_rules"
        android:icon="@mipmap/ic_launcher"
        android:label="@string/app_name"
        android:roundIcon="@mipmap/ic_launcher_round"
        android:supportsRtl="true"
        android:theme="@style/Theme.RaffleScreenNew">

        <!-- MainActivity is declared as the main activity -->
        <activity
            android:name=".MainActivity"
            android:exported="true">
            <intent-filter>
                <action android:name="android.intent.action.MAIN" />
                <category android:name="android.intent.category.LAUNCHER" />
            </intent-filter>
        </activity>

        <!-- Other activities without the launcher intent filter -->
        <activity
            android:name=".view.CartActivity"
            android:exported="false" />
        <activity
            android:name=".view.DetailedActivity"
            android:exported="false" />
        <activity android:name=".ForgotpassActivity" />
        <activity android:name=".CreateAccount" />
        <activity android:name=".AdapterClass" />

        <activity
            android:name=".StoreFrontActivity"
            android:exported="false" />
        <activity
            android:name=".view.DetailedActivityShop"
            android:exported="false" />

    </application>

</manifest>
